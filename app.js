$(document).ready(function(){
    $('.food-slider').slick({
        autoplay:true,
       slidetoshow:3,
       slideToScroll:1,
       prevArrow:".prev-btn",
       nextArrow:".next-btn",

    });
});